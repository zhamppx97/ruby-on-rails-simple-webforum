<% @forums.each do |forum| %>
<div class="col-md-4">
   <div class="thread">
      <h2 class="thread-title"><%= forum.thread %></h2>
      <%= link_to "View Forum Thread", forum_path(forum), class: "btn btn-default" %>
   </div>
</div>
<% end %>
<%= link_to "New Forum Thread", new_forum_path %>
